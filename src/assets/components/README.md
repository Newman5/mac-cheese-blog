# Reusable Components

This directory is for Eleventy reusable components using the eleventy-plugin-reusable-components.

## Usage

Create `.njk` component files here and use them throughout your Eleventy site.

For more information, see: https://github.com/copilot-swe-agent[bot]/eleventy-plugin-reusable-components
